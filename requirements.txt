# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.2.2
pydantic[email]==2.10.3
pydantic-settings==2.7.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1

# HTTP Client (async calls to AI APIs)
httpx==0.28.1

# SSE Streaming
sse-starlette==2.1.3

# Auth / JWT
PyJWT[crypto]==2.13.0
slowapi==0.1.9
cachetools==5.5.2

# DLP — NER de nomes próprios (modelo instalado via `python -m spacy download pt_core_news_sm`)
spacy==3.8.15

# Redis
redis==5.2.1

# pgvector (cache semântico)
pgvector==0.3.6

# Email
sendgrid==6.11.0

# File uploads — text extraction
python-multipart==0.0.31
pdfplumber==0.11.10
python-docx==1.1.2
openpyxl==3.1.5

# Utilities
orjson==3.11.6
tenacity==9.0.0

# Rastreamento de erro
sentry-sdk[fastapi]==2.20.0

# Observabilidade (Arize Phoenix / OpenTelemetry)
arize-phoenix-otel==0.17.1
